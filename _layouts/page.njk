<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Eli Goodman :: {{ title }}</title>
    <style>
      h2 {
        font-weight: bold;
        margin-bottom: 8px;
      }

      * {
        font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
          "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif,
          "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol",
          "Noto Color Emoji";

        color: #313131;
      }

      #content {
        max-width: 800px;
      }

      #container {
        display: flex;
        flex-direction: row;
        justify-content: center;
      }

      ul {
        margin-left: 0;
        padding-left: 0;
        list-style: none;
      }

      li {
        margin-bottom: 16px;
      }

      a {
        color: #5b98f5;
      }

      a:visited {
        color: #533383;
      }
    </style>
  </head>
  <body>
    <div id="container" class="flex flex-row p-6 justify-center">
      <div id="content" class="max-w-[800px]">{{ content | safe }}</div>
    </div>
  </body>
</html>
